# ============================================
# Queue Cure '26 — Client Environment Variables
# Vite requires the VITE_ prefix to expose vars to the browser bundle.
# ============================================

# URL of the backend Socket.IO + REST server
VITE_SERVER_URL=http://localhost:4000
